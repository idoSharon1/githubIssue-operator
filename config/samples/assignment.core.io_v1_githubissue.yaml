apiVersion: assignment.core.io.assignment.core.io/v1
kind: GithubIssue
metadata:
  labels:
    app.kubernetes.io/name: githubissue-operator
    app.kubernetes.io/managed-by: kustomize
  name: githubissue-sample
spec:
  # TODO(user): Add fields here
